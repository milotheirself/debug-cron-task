const config = {
  // [...]
};

export default config;

export const DEPLOY_BASE_ROUTE = '/example-page-a';

export const DEPLOY_SUBPATHS: Record<string, unknown> = {
  'example-page-a': {},
  'example-page-b': {},
  'example-page-c': {},
  'example-page-d': {},
};
